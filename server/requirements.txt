fastapi==0.141.1
uvicorn==0.52.1
sqlalchemy==2.0.51
aiomysql==0.3.2
pymysql==1.2.0
redis==8.1.0
slowapi==0.1.10
google-genai==2.16.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-jose==3.5.0
passlib==1.7.4
bcrypt==3.2.2
python-dotenv==1.1.1
email-validator==2.3.0
httpx==0.28.1

# File uploads: product images + Excel import
python-multipart==0.0.32
openpyxl==3.1.5
pillow==11.3.0

# Payments (real Stripe integration behind PAYMENT_PROVIDER=stripe; mock is the default)
stripe==11.6.0

# Transactional email (password reset, etc.) behind EMAIL_PROVIDER=smtp; console-log is the default
aiosmtplib==3.0.2

# Shipping (Israeli courier integration; see app/services/shipping_service.py).
# israel-shipping-sdk is vendored into this monorepo at packages/israel-shipping-sdk
# (its own standalone project, with its own tests/CI) -- installed editable so
# local development picks up changes without a rebuild. No package registry is
# set up for it yet; if that changes, replace this line with a pinned version
# from that registry instead of the local path.
cryptography==46.0.0
-e ../packages/israel-shipping-sdk
respx==0.23.1  # test-only: mocks the courier HTTP calls in tests/test_shipping.py
